# CO2 Calculation Tool

This is a code bundle for CO2 Calculation Tool. The original project is available at https://www.figma.com/design/MAVj2wWI6G7QaA3BWgRcHU/CO2-Calculation-Tool.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.